{
  "devDependencies": {
    "cypress": "^12.9.0",
    "dotenv": "^16.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4",
    "husky": "^8.0.0"
  },
  "scripts": {
    "test": "npx ts-node ./scripts/e2e.ts",
    "prepare": "husky install",
    "checkUi": "cd src/chainlit/frontend && npm run lint && npm run format",
    "checkPython": "black src",
    "installUiDeps": "cd src/chainlit/frontend && npm install"
  }
}
