[dependency-groups]
backend = ["chainlit"]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.uv]
default-groups = ["backend"]
package = false

[tool.uv.sources]
chainlit = { workspace = true }

[tool.uv.workspace]
members = ["backend"]